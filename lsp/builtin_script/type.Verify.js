/**
 * @httpyac-type Verify
 * From crypto.createVerify
 */
'use strict';

module.exports = {
  /**
   * @returns {Verify}
   */
  update(data, inputEncoding) {},
  /**
   * @returns {boolean}
   */
  verify(publicKey, signature, encoding) {},
};
