/** Builtin Node `Buffer` static API. */
'use strict';

module.exports = {
  from(data, encoding) {},
  alloc(size) {},
  allocUnsafe(size) {},
  concat(list) {},
  isBuffer(obj) {},
  byteLength(string, encoding) {},
};
